<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.idat.farmacias">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Farmacias">

            <!--android:name=".ListadoEnfermeros"
            android:label="@string/title_activity_listado_enfermeros">-->
        <activity
            android:name=".ListadoMedicamentos"
            android:label="@string/title_activity_listado_medicamentos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>